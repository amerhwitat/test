# Web Implementations

The repository now includes a browser smoke-test harness. It is intended for validating baseline browser APIs and future web components, service workers, TypeScript and WebAssembly adapters.